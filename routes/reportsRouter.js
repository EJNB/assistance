let express = require('express'),
    router = express.Router(),
    reportController = require('../controllers/reportsController');

// router.get('/',reportController.getAll);

module.exports = router;